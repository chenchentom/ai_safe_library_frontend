    const res = await searchRiskClue(params as any)
    console.log('=== 后端返回的完整数据 ===', res)
    const data = res as any
    const rawList = data?.rows || data?.records || data?.list || data?.data || []
    console.log('=== 原始数据列表 ===', rawList)
    const normalized = rawList.map(normalizeClueData)
    console.log('=== 规范化后的数据 ===', normalized)
    clueList.value = normalized
